<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>OrderService</RootNamespace>
    <AssemblyName>OrderService.InAppOutbox</AssemblyName>
    <Description>
      Sample ASP.NET Minimal API that demonstrates the Transactional Outbox pattern
      using Hermodr: order domain events are first persisted to a SQLite outbox
      (via EF Core) and then forwarded to RabbitMQ by a relay BackgroundService running
      inside the same application process.
    </Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="9.0.5" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" Version="9.0.5" />
  </ItemGroup>

  <ItemGroup>
    <!-- Hermodr: outbox pattern (EF Core) + RabbitMQ transport channel -->
    <ProjectReference Include="..\..\..\src\Hermodr.Publisher.Outbox.EntityFramework\Hermodr.Publisher.Outbox.EntityFramework.csproj" />
    <ProjectReference Include="..\..\..\src\Hermodr.Publisher.RabbitMq\Hermodr.Publisher.RabbitMq.csproj" />
  </ItemGroup>

</Project>

